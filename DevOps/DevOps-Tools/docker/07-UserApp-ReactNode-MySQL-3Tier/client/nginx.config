server {
    listen 80;
    # If Left as _ (underscore): Nginx will treat it as the default server for unmatched requests. This is fine for local development but not ideal for production.
    server_name localhost;

    # Serve static files from React/Vue/Angular frontend
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    # Proxy API requests to Node.js
    location /api/ {
        proxy_pass http://server:5000; # Use the Docker Service name instead of localhost or IP address 
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
